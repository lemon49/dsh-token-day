# dsh-hot-restart bundle patch: inserts this plugin's host row into the
# profile tree. The client half (`lib/client.js`) is discovered separately
# through the `dsh.client` declaration in package.json.
- insert:
    - id: hot-restart
      name: 'dsh-hot-restart'